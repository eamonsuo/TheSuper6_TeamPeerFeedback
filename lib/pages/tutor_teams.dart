import 'package:flutter/material.dart';

class TutorTeamsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Tutor Teams')),
      body: Center(child: Text('Tutor Teams Page')),
    );
  }
}
